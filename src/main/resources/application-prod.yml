server:
  port: 8101
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: x
    username: x
    password: x
  # Redis 配置
  # todo 需替换配置
  redis:
    database: x
    host: x
    port: x
    timeout: x
    password: x
  # Elasticsearch 配置
  # todo 需替换配置
  elasticsearch:
    uris: x
    username: x
    password: x
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''